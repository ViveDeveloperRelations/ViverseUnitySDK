using System.Threading.Tasks;
using UnityEngine;
using ViverseWebGLAPI;

namespace Tests.TestEndpoints
{
    public class WebRequestAPIMock1
    {
        /*
        public async Task<Profile> GetProfile(WorldHost worldHost)
        {
            Debug.Log($"GetProfile");
            Profile profile = null;
            await UnityWebRequestGet($"{GetWorldHost()}api/hubs-cms/v4/me", true,
                (code, res) =>
                {
                    if (code == 200)
                    {
                        profile = JsonUtility.FromJson<Profile>(res);
                        //Debug.Log($"Getavatarlist CurrentAvatarId:{avatarlist.data.CurrentAvatarId}");
                    }
                });
            return profile;
        }
        */
    }
}